[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "bash -c 'DATABASE_URL=postgresql://user:pass@localhost:5432/db npx prisma generate && npm run build:backend'"
]

[start]
cmd = "npx prisma migrate deploy && node dist/main"
